# Разбор ветки demo-ui, 10.09.2026

Накопилось 41 изменённый файл, 27 новых, 8 удалённых и 3 419 сгенерированных
без учёта. Здесь разобрано, зачем изменён каждый, и предложено разбиение на
коммиты. Ничего не восстановлено и не удалено: это опись, а не уборка.

## Страховка

Снимок рабочего дерева целиком (кроме `outputs/`) лежит под меткой
`snapshot-2026-09-10`, коммит `9a49a16`. HEAD не двигали, рабочее дерево не
трогали: метка существует отдельно от ветки.

```bash
git show --stat snapshot-2026-09-10                  # что в снимке
git checkout snapshot-2026-09-10 -- <файл>           # вернуть один файл
```

## Осознанная функциональность

| Файл | Зачем изменён |
| --- | --- |
| `src/fot_planner/optimizer.py` | Человеко-месяцы: ключи только там, где есть выплаты (расчёт с 690 с до ~100 с); `LABOR_CLOSE_TIEBREAK` 0,01 → 1,0 против дробных ставок в отчёте; правило остатка ФОТ и стадия «число договоров с остатком» — от параллельного агента, спорное |
| `src/fot_planner/validation.py`, `labor_rules.py`, `models.py` | Сопровождающие правки под то же |
| `src/fot_planner/excel/*` | Разбор входа и лист `ШР_детализация` |
| `app/build_input.py` | Окно выплат договора: человеко-месяцы из месяцев после предельного срока переносятся в оплачиваемые, в предупреждениях появляется строка |
| `app/rules.py` | Причина решений берётся из атрибуции решателя, а не угадывается; чтение всех строк РКМ договора; `PM_EPS` |
| `app/chat.py` | Подсказка агента: не выдумывать чисел, на возражение разбирать заново, не переписывать прежний ответ; в состояние добавлены строки РКМ и разбор зарплаты |
| `app/main.py` | Реплика и документ обрабатываются синхронно (было `async def`, блокировало цикл); защита от второго расчёта; статус «прерван»; кэш предпросмотра по `sha256`; `_warm_pdf` в отдельном потоке; забывание источника при удалении документа |
| `app/reference.py` | В справочнике хранится и исходное, и производное значение — иначе в П4 нечего подсвечивать; `forget_source` |
| `app/docread.py`, `app/intake.py`, `app/ocr.py` (новый) | Чтение сканов: страница PDF → картинка → модель → текст |
| `app/llm.py` | `VISION_MODELS = {}`: модель `deepseek-v4-flash-vision-exp` не существует |
| `app/db.py`, `app/report.py`, `app/export_report.py` (новый) | Выгрузка плана в Excel теми же разделами, что на вкладке |
| `app/static/app.js`, `app.css`, `index.html` | Лента: реплика видна сразу и признак работы; шапки таблиц; перенос широких граф; штриховка; подсветка в документах; проценты отклонения |
| `app/static/issues.js`, `issues.css` | Единый список замечаний, подключён в `index.html` — от параллельного агента |
| `.claude/settings.json`, `scripts/lint_changed.py`, `scripts/stop_gate.py`, `scripts/parts.py` | Обвязка: запреты, проверка синтаксиса после правки, ворота на Stop |
| `scripts/check.py` | Один вход в приёмку, три скорости, учёт известного красного |
| `.github/workflows/priyomka.yml` | Приёмка вне машины автора |
| `scripts/create_demo_docs.py`, `scripts/demo_run.py`, `scripts/demo_offline.py` | Демо-набор 10 человек / 5 договоров: ГОЗ с трудоёмкостью и БЭП, коммерция под 124 и П4, грант, приоритет со 152; поэтапные поступления |
| `pyproject.toml` | Зависимости под сканы и веб |

## Тесты и стенд

| Файл | Что с ним |
| --- | --- |
| `harness/crisis.py`, `harness/cases.py`, `harness/run.py` | Случаи правил решателя и разбора |
| `harness/chat.py` (новый) | 18 случаев поведения чата, без сети |
| `harness/known_red.txt` (новый) | 09 и 26: разобранная причина, ждут решения |
| `harness/crisis_template.xlsx` (новый) | Заготовка входа для случаев; нужен `crisis.py` |
| `tests/conftest.py` | Больше не выдаёт «успех» при нуле выполненных тестов |
| `tests/test_contract_remainders.py` (новый) | Тест правила остатка — от параллельного агента; проверяет спорное правило |
| `tests/test_employment_calendar.py`, `test_extract_fot_detail.py` (новые) | Запускаются только с `--noconftest`, в приёмку не входят |
| `scripts/test-issues.cjs` (новый) | Проверка списка замечаний на Node — от параллельного агента, ни в `check.py`, ни в CI |

## Документация

| Файл | Что с ним |
| --- | --- |
| `CLAUDE.md` (новый) | Границы и приёмка для агентов |
| `docs/ПРАВИЛА_РЕШАТЕЛЯ.md` (новый) | 25 жёстких правил и 9 стадий целей, каждое привязано к случаю |
| `docs/ЧАТ_ПРАВИЛА.md` (новый) | 19 правил ленты, привязаны к случаям ч01–ч17 |
| `docs/ЗАЩИТА_ДЕМО_2026-09-10.md` (новый) | Сценарий показа |
| `docs/CONTRACT_REMAINDERS.md` (новый) | Правило остатка ФОТ — от параллельного агента, спорит с ревью, раздел 9 |
| `docs/ISSUE_UI.md`, `docs/DOCVIEW.md` (новые) | Контракт списка замечаний и просмотр документов |
| `docs/MODEL_REVIEW.md`, `docs/CONSISTENCY.md`, `README.md` | Правки по ходу; ревью разъехалось со сводом правил на восемь пунктов |

## Сгенерированное

| Что | Сколько | Что делать |
| --- | --- | --- |
| `outputs/real_case_20260909/` | 3 419 файлов, 2,9 МБ | В `.gitignore`: пересоздаётся прогоном |
| `docs/ui/demo_input.xlsx` | 1 файл | Собирается `docs/ui/extract.py` |
| `data/demo/*.xlsx`, `data/demo_variants/дефицит/*.xlsx` | 8 файлов | Собираются `scripts/create_demo_docs.py`; в репозитории лежат намеренно — их грузят руками на показе |

## Удаления

| Что удалено | Объяснение |
| --- | --- |
| `data/demo_variants/п4/` (4 файла) | Варианты старого набора 8 человек / 4 договора. Генератор их больше не делает: `VARIANTS = ("базовый", "дефицит")` |
| `data/demo_variants/совместители/` (4 файла) | То же |

Оба удаления объяснимы, но решение за вами: восстановить из `HEAD` эти файлы
как есть нельзя — они описывают прежний набор данных. Либо удаляем совсем,
либо добавляем варианты в генератор и делаем заново.

## Временный мусор

Не нашёл. `app/static/issues-demo.html` и `issues-demo.js` — не мусор, а
отдельная демонстрационная страница списка замечаний; в приложение она не
подключена и используется тестом `scripts/test-issues.cjs`.

## Предлагаемое разбиение на коммиты

1. **Решатель: скорость и целые ставки** — `optimizer.py`, `labor_rules.py`,
   `models.py`, `validation.py`, `app/build_input.py`, `app/rules.py`,
   `harness/crisis.py`, `harness/cases.py`.
2. **Правило остатка ФОТ договора** — отдельно, целиком: `docs/CONTRACT_REMAINDERS.md`,
   `tests/test_contract_remainders.py` и его часть в `optimizer.py`. Отдельный
   коммит потому, что правило спорное и его, возможно, придётся снять целиком.
3. **Чат: разбор заново, состояние плана** — `app/chat.py`, `app/main.py`
   (часть про ленту), `harness/chat.py`, `docs/ЧАТ_ПРАВИЛА.md`.
4. **Документы: сканы, предпросмотр, справочник** — `app/ocr.py`,
   `docread.py`, `intake.py`, `reference.py`, `llm.py`, `docs/DOCVIEW.md`,
   `pyproject.toml`.
5. **Интерфейс плана** — `app/static/*` без `issues*`.
6. **Список замечаний** — `issues.js`, `issues.css`, `issues-demo.*`,
   `scripts/test-issues.cjs`, `docs/ISSUE_UI.md`.
7. **Демо-набор 2026** — `scripts/create_demo_docs.py`, `demo_run.py`,
   `demo_offline.py`, `data/demo/*`, `docs/ЗАЩИТА_ДЕМО_2026-09-10.md`,
   удаление старых вариантов.
8. **Обвязка приёмки** — `CLAUDE.md`, `.claude/settings.json`, `scripts/parts.py`,
   `check.py`, `stop_gate.py`, `lint_changed.py`, `tests/conftest.py`,
   `.github/workflows/priyomka.yml`, `harness/known_red.txt`,
   `docs/ПРАВИЛА_РЕШАТЕЛЯ.md`, этот файл.
9. **Выгрузка отчёта в Excel** — `app/export_report.py`, `app/report.py`,
   `app/db.py`.
