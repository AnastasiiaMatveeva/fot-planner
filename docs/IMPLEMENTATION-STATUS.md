# Развитие агентов: состояние реализации

## Основание и область

Пользователь передал ТЗ руководителя и поручил начать работу по нему 11.09.2026.
Источник: [ТЗ_РАЗВИТИЕ_АГЕНТОВ.md](ТЗ_РАЗВИТИЕ_АГЕНТОВ.md),
`FOT-HARNESS-TZ-001`, версия `0.1.0`. Текст источника сохранён без изменений.
SHA-256 исходного и сохранённого файла:
`3b72157bb3e98b3e8eef4bfa2439a3b17b6f156afa0dd8ec7b6a7fbcb940b82f`.

Поручение начинает локальную работу; оно не означает, что все новые требования
уже реализованы или все предметные гипотезы утверждены. Их исходные статусы
сохраняются по разделу 4 ТЗ. Самостоятельность demo-ui обязательна для L0–L5.
I1/I2, внешние передачи и миграция живой базы не входят в текущий старт работ.

Этот файл — изменяемый журнал продолжения, а не второй текст ТЗ. Подробные
формулировки и критерии остаются в исходнике и предметных документах.
ID требований записываются с областью, например `FOT-HARNESS-TZ-001/MEM-002`.

## Уточнения к источнику

1. **Порог остатка 1 000 ₽.** Разделы 5.2 и 13 ТЗ называют его спорным legacy.
   Пользователь уже поручил убрать его; текущий статус записан в
   [CONTRACT_REMAINDERS.md](CONTRACT_REMAINDERS.md). При работе по ТЗ сохраняется
   отмена порога. Требования ТЗ не разрешают возвращать его техническим переносом.
2. **Текущая версия новее исходника ТЗ.** После указанного в ТЗ commit появились
   табельный номер, жёсткий допуск трудоёмкости, расширенные проверки и другие
   изменения. Их нельзя потерять при переносе подходов из другой ветки.
3. **Совпадающие ID.** `UI-001` ТЗ и `UI-001` правил интерфейса — разные
   требования. В будущей машинной матрице ключ включает ID документа.
4. **Внешние источники.** Документы finance/astra-harness по ссылкам ТЗ в этом
   старте не проверены. Совместимость с их контрактами не подтверждена.
   Перед использованием конкретного внешнего контракта нужно прочитать его
   зафиксированную версию; локальная работа продолжается по доступному ТЗ.

## Пакеты

| Пакет | Состояние | Следующее обязательство |
| --- | --- | --- |
| L0 | done: инвентаризация, карта требований, ADR и baseline (L0.3) | Проверить критерии L0 и начать L1 |
| L1 | done с долгом (L1.5): прогоны, миграции, снимок, решения, актуальность, повтор, ключи операций; долг перечислен в checkpoint L1.5 | Начать L2: каталог способностей, применимость, область исправлений и версионные эталоны |
| L2 | not_started | Каталог, применимость, область исправлений и версионные эталоны |
| L3 | частично: QA-003 существующего harness; остальное not_started | Полнота предметных проверок, раздельные статусы, сравнение кандидатов |
| L4 | not_started | Запросы улучшений, испытания, активация/откат и учёт пробелов |
| L5 | not_started | Сквозная приёмка автономного продукта, перенос, UI и производительность |
| I1/I2 | out_of_scope | Отдельная интеграционная поставка по поручению |

T-QA-003 частично проверяется в существующем CLI (см. L0.2 ниже); квалификация
версий способностей пока не реализована. Остальные новые T-* из раздела 10 ТЗ
остаются `not_run`. Исходный прогон harness не заменяет эти сценарии.

## Checkpoint L0.1 — регистрация и начальная сверка

- Дата: 11.09.2026. Исполнитель проверки: Codex; не предметный утверждающий.
- Requirement IDs: `FOT-HARNESS-TZ-001/DOC-001`, `DOC-002` — частичная работа.
- Фактический исходный HEAD: `94d609672ed73745338c86d6b48e9b408287c275`.
- Ветка: `demo-ui`. Перед началом рабочее дерево чистое.
- Исходный commit ТЗ: `0a7a091ccffb45777c668602a29d8bde7e3ac9fc`.
- Сравнение этих commit: 55 файлов, 1 043 добавления, 5 229 удалений.
  Это свидетельство расхождения версий, не полный аудит каждой строки.
- Добавлены исходное ТЗ, этот журнал и маршрут к ним в AGENTS/индексе.
- Изменений runtime, схемы живой БД и интерфейса в L0.1 нет.

### Первые наблюдения по существующим точкам исполнения

| Область | Свидетельство в коде | Пробел для следующей части |
| --- | --- | --- |
| Каталог | `app/agents.py::AGENTS`, `SOLVER` | `real` и текст `does` не доказывают qualification; нужен обход обработчиков каждой роли |
| Версии | `app/agents.py::_prompt_texts`, `version_of` | Короткий SHA-1 подсказок intake/norms; прочим возвращается «—». Полного состава code/tool/model/profile/checks нет |
| Исправления | `app/db.py::Correction`, `Verdict`; `app/llm.py` | Есть безусловная подсказка «правки важнее общих правил». MEM-001/002 требуют проверяемой области и приоритета |
| Расчёт | `app/main.py::_solve`, `src/fot_planner/planner.py` | Нужно проверить pins, окончательный hash после всех преобразований и восстановление |
| Проверка | `result_audit.py`, `app/rules.py`, `_review_result` | Имеющиеся проверки и пересказ моделью не равны полной QA-001/002; нужны применимость и независимые отрицательные случаи |
| Стенд | `scripts/check.py`, `scripts/parts.py` | Текущий режим допускает known_red в общем успехе; отдельного доказанного режима квалификации пока нет |

Наблюдения сделаны чтением кода. Для остальных ролей и полного покрытия
статус инвентаризации — `not_verified`, без вывода об отсутствии реализации.

### Команды и свидетельства

Команды выполняются из корня проекта на Windows, Python 3.11.9, Node v20.17.0.

```powershell
git -c safe.directory=E:/ProjectHub/PycharmProjects/fot-planner status --short
git -c safe.directory=E:/ProjectHub/PycharmProjects/fot-planner log -1 --format='%H %s'
git -c safe.directory=E:/ProjectHub/PycharmProjects/fot-planner diff --stat 0a7a091ccffb45777c668602a29d8bde7e3ac9fc HEAD
.venv/Scripts/python.exe scripts/check.py --quick
```

Git-команды завершились с кодом 0. `safe.directory` задан только для конкретных
вызовов чтения; настройки Git пользователя не менялись.
Быстрый прогон завершён с кодом **0**: чат 18/18, аудит 15/15, решатель 31/31;
хуки и проверки списка замечаний также прошли. Фактический вывод:
[L0.1-quick.txt](evidence/FOT-HARNESS-TZ-001/L0.1-quick.txt).
SHA-256 журнала: `c0a44f9e1c96cb30184bbc451ff014a7a5d085afcc10afc927a97d57a04b7278`.
Это синтетические случаи и подставленные ответы LLM, не оценка качества живой
модели. Новые T-* и полный сквозной прогон остаются `not_run`.
Копия исходного ТЗ сверена по SHA-256; локальные ссылки проверены, ошибок нет.

### Что нужно для завершения L0

1. Дополнить инвентаризацию всех девяти ключей раздела 7: handler, реальные
   входы/выходы, зависимость от LLM, тесты и ограничения. Проверить маршруты,
   а не переносить `does` как доказательство.
2. Составить машиночитаемую карту всех ID ТЗ и текущих пробелов. Связать
   предметные требования с действующими карточками, положительными и
   отрицательными случаями; незапущенные случаи оставить `not_run`.
3. Записать `ADR-local-harness.md`: SQLite и хранилище неизменяемых артефактов,
   локальный service layer, формат canonical JSON, интерфейс optional adapter.
   Зафиксировать имена будущих профилей CLI до их реализации.
4. Закрепить входы и среду для performance baseline; выполнить три одинаковых
   прогона без LLM. Предлагаемый предел ТЗ: медиана после изменения не хуже
   исходной более чем на `max(1 с, 10% baseline)`. Сейчас замеры `not_run`;
   одиночный быстрый прогон не является performance baseline.

Следующая конкретная работа: пункт 1, затем карта требований и ADR. L1 начинается
после проверки критериев L0, а не после одного создания файлов.

Откат L0.1: убрать добавленные документы и их навигационные ссылки. Миграция,
перезапуск сервиса и откат пользовательских данных для этих изменений не нужны.

## Checkpoint L0.2 — защита достоверности исходных проверок

Дата: 11.09.2026. Поручение: продолжить практические улучшения по ТЗ.
До завершения полной инвентаризации выделена узкая исправляющая часть QA-003:
старый CLI мог списать аварийное завершение на known_red, а Stop и CLI
по-разному трактовали один вывод. Исправление этой ошибки существующей
приёмки не требует новых моделей БД и не означает прохождения зависимостей
L1/L2 или готовности L3 целиком.

Requirement IDs: `FOT-HARNESS-TZ-001/QA-003` — частично реализован;
`QA-002`, `DOC-002` — ограниченные свидетельства текущего CLI.

Изменения:

- `scripts/check_results.py`: единый анализ кода возврата и протокола для CLI
  и Stop; отдельные runtime/evaluation/acceptance, известные случаи сохраняют
  `failed`, разработческий итог — максимум `baseline_compatible`.
- `--strict` не прощает known_red. Пустой набор — `not_run`. Ошибки запуска,
  несогласованные счётчики/коды, незавершённый вывод и traceback блокируют
  приёмку. Номер known_red относится только к crisis, а не ко всем стендам.
- `--report PATH`: атомарно записываемый JSON с областью запуска, run_id,
  командами, временем, exit code, случаями и SHA-256 вывода/точки входа.
  Сырые выводы стендов, `.env` и содержимое документов в него не включаются.
- 22 регрессии в `scripts/test_check_results.py`, подключённые через parts
  к quick, выборке изменений, Stop и CI; не через отключённый pytest.
- CI настроен на строгий quick и сохранение JSON-артефакта даже при провале.
  Сам GitHub job локальным прогоном не проверяется: до push он `not_run`.
- При недоступном git режим changed/quick не начинает обращаться к LLM.

### Проверки L0.2

Сначала новый тестовый скрипт завершился кодом 1: отсутствовал модуль
`check_results`. После реализации 17 первичных регрессий прошли; затем
добавлены ещё 5 проверок пустой выборки, сохранения отчёта и подключения ворот.
Прямой запуск всех 22 регрессий завершился кодом 0. Они используют подставные
протоколы и процессы; отдельно реально запускаются Python и отсутствующий
исполнитель. После дополнений полный строгий quick завершился кодом **0**:
22 регрессии ворот, чат 18/18, аудит 15/15, решатель 31/31, хуки и Node — passed.

```powershell
.venv/Scripts/python.exe scripts/test_check_results.py
.venv/Scripts/python.exe scripts/check.py --quick --strict --report docs/evidence/FOT-HARNESS-TZ-001/L0.2-quick.json
git -c safe.directory=E:/ProjectHub/PycharmProjects/fot-planner diff --check
```

Свидетельство: [L0.2-quick.json](evidence/FOT-HARNESS-TZ-001/L0.2-quick.json),
run_id `4e676876-0344-45e8-baa3-acec0dfbbca9`. JSON прочитан обратно, итог
`passed`, exit_code 0. `git diff --check` — код 0. Исходное ТЗ осталось
неизменным по SHA-256. Эти прогоны не являются тройным performance baseline.

Ограничения: это не неизменяемый execution snapshot VER-002 и не полный
qualification evidence QA-002. Хешируется только точка входа, не весь код
и зависимости. Три существующих стенда дают поштучный протокол, остальные
пока проверяются по коду процесса и непустому выводу. Реестр обязательного
набора QA-001 ещё предстоит сделать. Структурированные владелец/область/дата
известного дефекта пока не введены: существующий текстовый формат сохранён.
Stop сохраняет старый десятиминутный кеш и не доказывает проверку каждого
снимка. Поэтому QA-003 целиком не помечен `qualified`.

Следующий пакет работы: завершить пункты L0 — инвентаризацию обработчиков,
машинную карту требований/пробелов, ADR и воспроизводимый performance baseline.
Экономические правила, UI, `app/data/` и `.env` этим изменением не затронуты.
Закоммичено 11.09.2026 (`2e509a4`) после строгого quick: шесть частей passed.

## Checkpoint L0.3 — инвентаризация ролей, карта требований, ADR, baseline

Дата / исполнитель: 11.09.2026, Claude (Fable); не предметный утверждающий.
Пакет / часть / requirement IDs: L0 целиком; `FOT-HARNESS-TZ-001/CAP-002`
(инвентаризация), `QA-001` (карта, частично), `NFR-001` (baseline измерен),
`DOC-002`.
Исходный commit / состояние дерева: `2e509a4`, дерево чистое.

### Инвентаризация девяти ключей раздела 7

Проверено чтением маршрутов `@app.*` в `app/main.py`, вызовов
`agents.working / say / handoff` и обработчиков; не по тексту `does`.

| Ключ | Обработчик | Модель | Проверки | Вывод |
| --- | --- | --- | --- | --- |
| intake | `_process`, `intake._store_context` (шаблон, без модели), `intake._store_passport` + `llm.freeform` (незнакомая форма), `decide_proposals` | optional | `harness/run.py` 10 случаев (нужна модель); `demo_offline` для шаблона | implemented |
| tuning | `post_message` → `chat.reply` (модель классифицирует) → `parse_settings/parse_months/apply_settings` (код) | optional | ч05–ч17 | implemented |
| solver | `_solve` → CLI `fot-planner solve` → `optimizer.solve` + `result_audit.audit` | none | 31 crisis, 15 audit, demo_offline | implemented |
| infeasible | `_explain_failure`: сверка с реестром и опыты решателем с одним ослабленным условием | none | нет автоматического случая; только demo_run.py по API | implemented, tests not_run |
| scenario | **нет**: ни маршрута, ни вызова — только запись в каталоге | — | нет | catalogue_only, `real=True` неверно |
| norms | `intake.run_norms` → дифф справочника → `POST /api/document/{id}/reference` | required | случаи разбора приказа/справки/письма (нужна модель) | implemented |
| checker | `_review_result`: пакет из 16 условий `app/rules.py` + предупреждения; модель только пересказывает | optional | нет | implemented, tests not_run |
| memo | **нет**: только запись в каталоге | — | нет | catalogue_only, `real=True` неверно |
| reconcile | нет | — | нет | declared, `real=False` честно |

Что переиспользовано: существующие стенды и каталог; ничего в коде
обработчиков не менялось.

Файлы и версии:
- `docs/requirements-map.json` — карта: 9 способностей, 40 требований ТЗ
  (все ID раздела 5), связь свода правил с случаями через сам свод;
- `scripts/check_requirements_map.py` — валидатор карты, часть приёмки
  «карта требований» (`scripts/parts.py`), протокол `cases-v1`;
- `docs/architecture/ADR-local-harness.md` — хранилище, service layer,
  канонический JSON `fot-json-v1`, версии, каталог, имена профилей CLI,
  порог NFR-001;
- `docs/evidence/FOT-HARNESS-TZ-001/L0.3-baseline.json` — три прогона.

Команды / среда / exit codes (Windows 10, Python 3.11.9):

```text
.venv/Scripts/python.exe scripts/check_requirements_map.py            → 0, 6 из 6
.venv/Scripts/python.exe scripts/check_requirements_map.py --map <испорченная копия> → 1, 5 провалов из 6
scripts/demo_offline.py --limit 240 --out <run1..3>                    → 0 ×3, OPTIMAL
.venv/Scripts/python.exe scripts/check.py --quick --strict --report docs/evidence/FOT-HARNESS-TZ-001/L0.3-quick.json
```

Положительные случаи: к01–к06 на действующей карте; три прогона baseline
с одинаковой суммой выплат 14 793 893 ₽.
Отрицательные случаи: испорченная копия карты (выдуманный случай ч99,
пропущенный INT-003, несуществующий файл, `implemented` без обработчика,
скрытое расхождение `real` с каталогом) — валидатор краснеет по каждому
пункту, к01–к05 ПРОВАЛ.
Evidence refs / hashes: `L0.3-baseline.json` (медиана решателя 28,6 с,
предел 31,46 с), `L0.3-quick.json` (run_id внутри).
Миграция / restart / rollback: не требуются; откат — удалить четыре файла и
строку части в `scripts/parts.py`.
Непройденное / not_run / known debt: T-* ТЗ по-прежнему `not_run`; у infeasible
и checker нет случаев; входной файл baseline не заморожен побайтно (см.
limitations в evidence).
Открытое предметное решение и ответственный: `real=True` у scenario и memo —
снять флаг или реализовать обработчик; решение владельца продукта.
Следующий конкретный шаг: L1 — миграции на изолированной копии, RunManifest
и артефакты по ADR, operation ID для `solve` и `message` (RUN-001),
исправление `_close_orphans` (RUN-002). Закоммичено `002c2d3`.

## Checkpoint L1.1 — владелец прогона, heartbeat, ключ операции запуска

Дата / исполнитель: 11.09.2026, Claude (Fable).
Пакет / часть / requirement IDs: L1, часть 1; `FOT-HARNESS-TZ-001/RUN-002`
(перезапуск не трогает чужой живой расчёт), `RUN-001` (ключ операции и
единственный вход запуска — только для расчёта).
Исходный commit / состояние дерева: `002c2d3`; в дереве чужой незакоммиченный
`scripts/test_change_guard.py` (Codex), не трогался.

Что было. `_close_orphans` при старте помечал «прерван» каждый прогон со
статусом «идет» — и живой расчёт другого процесса тоже; стенды спасались
только переменной `FOT_SKIP_ORPHANS`. Прогон заводился в четырёх местах
(кнопка, претензия, закрепление, «посчитай»), каждое по-своему проверяло
занятость, и ни одно не записывало, кто считает.

Что сделано:
- `Run.executor` (хост:pid:метка), `Run.heartbeat`, `Run.operation_id` —
  графы добавляются существующим механизмом `_ADDED_COLUMNS` при старте;
  нумерованных миграций с dry-run пока нет (MIG-002 — следующая часть L1);
- `_heartbeat` в отдельном потоке обновляет метку раз в 15 с, пока идёт
  `_solve`; `_close_orphans` признаёт брошенным прогон, молчащий дольше 60 с,
  а строки работы агентов закрывает лишь при отсутствии живых прогонов;
- `_start_run` — единственный вход: занятый план возвращает идущий прогон;
  `operation_id` из тела `POST /api/case/{id}/solve` делает повтор безопасным,
  а тот же ключ с другими данными — конфликт 409.
- `scripts/demo_run.py` читает адрес сервера из `FOT_BASE_URL`, чтобы гонять
  сквозной сценарий на временной базе, не трогая живую.

Что переиспользовано: `_running_run`, механизм добавления граф, стенд чата
как образец временной базы.

Файлы и версии: `app/main.py`, `app/db.py`, `harness/lifecycle.py` (л01–л07),
`scripts/parts.py` (часть «жизненный цикл прогонов»), `scripts/check_results.py`,
`scripts/check_requirements_map.py` (префикс «л»), `scripts/demo_run.py`,
`docs/requirements-map.json`, `docs/ТЕСТИРОВАНИЕ.md`.

Команды / среда / exit codes:

```text
.venv/Scripts/python.exe harness/lifecycle.py                  → 0, 7 из 7
.venv/Scripts/python.exe scripts/check.py --quick --strict     → 0, восемь частей passed
FOT_BASE_URL=http://127.0.0.1:8771 scripts/demo_run.py --title "L1.1 heartbeat"
    сервер на временной базе (FOT_DATA_DIR во временной папке), порт 8771 — см. ниже
```

Положительные случаи: л01 (живой чужой прогон не тронут), л03 (рядом с
брошенным), л04 (повтор ключа → тот же прогон), л06 (занятый план), л07
(heartbeat обновляется и останавливается).
Отрицательные случаи: л02 (замолчавший и старый без heartbeat → «прерван»,
строки работы закрыты), л05 (тот же ключ с другими данными → 409, второго
прогона нет).
Сквозной прогон на временной базе (сервер на порту 8771, `FOT_DATA_DIR` во
временной папке, живая база не тронута): четыре документа демо-набора
разобраны; прогон № 1 создан с исполнителем `2-61:29396:…` и heartbeat;
heartbeat обновлялся каждые 15 с всё время расчёта (21:25:54 → 21:35:40 и
дальше); повторный `POST /api/case/2/solve` с тем же `operation_id` вернул
тот же `run_id` с признаком «идет»; импорт `app/main.py` чужим процессом на
той же базе без `FOT_SKIP_ORPHANS` живой прогон не тронул
(`_close_orphans() → {"прервано": 0, "живых": 1}`) — до L1.1 он помечал его
«прерван». Замечание: расчёт запущен моей пробой ключа операции до того,
как разобрались РКМ и структура цены (план уже был «готово к расчету» по
штатке и договорам), поэтому вход прогона без строк РКМ, а `demo_run.py`,
ждущий тишины всех работ агентов, вышел по своему таймауту 600 с — это
следствие пробы, не дефект скрипта. Наблюдение для L3: план можно считать до
конца разбора всех загруженных документов. Прогон завершился OPTIMAL за
802 с (вход без строк РКМ решается заметно дольше демо с ними), последний
heartbeat 21:39:10, статус записан, поток heartbeat остановлен.
Миграция / restart / rollback: три графы добавляются при старте
существующим механизмом; откат — убрать графы из `_ADDED_COLUMNS` и модели,
старые строки без heartbeat считаются брошенными, как и раньше. Живая
`app/data/` не трогалась: сквозной прогон шёл на временной папке.
Непройденное / not_run / known debt: ключ операции нет у реплик чата и
принятия строк; Activity не привязана к прогону; T-RUN-001/002 из ТЗ не
написаны как отдельные -P/-N, покрытие через л01–л07.
Открытое предметное решение и ответственный: нет.
Следующий конкретный шаг: L1.2 — нумерованные миграции с dry-run на копии
базы (MIG-002) и RunManifest с артефактами входа/выхода по SHA-256 (VER-002).
Закоммичено `4b66b40`.

## Checkpoint L1.2 — нумерованные миграции: dry-run, копия, транзакции, откат

Дата / исполнитель: 11.09.2026, Claude (Fable).
Пакет / часть / requirement IDs: L1, часть 2; `FOT-HARNESS-TZ-001/MIG-002`
(реализовано), `MIG-001`, `MIG-003` (частично).
Исходный commit / состояние дерева: `4b66b40`; чужой незакоммиченный
`scripts/test_change_guard.py` не трогался.

Что было. Схема менялась только тем, что `app/db.py::init_db` при каждом
старте молча добавлял недостающие графы в живую базу: ни версии, ни
предпросмотра, ни копии, ни отката.

Что сделано:
- `migrations/` — нумерованные шаги на sqlite3 без моделей SQLAlchemy:
  001 (исходная схема: графы, добавлявшиеся при старте), 002 (исполнитель,
  heartbeat, ключ операции прогона). Шаг объявляет план (`plan`) и
  применение (`apply`), оба идемпотентны;
- `scripts/migrate.py`: `--dry-run` без записи, `--apply --backup DIR`
  (копия через API резервного копирования SQLite, применение без копии
  отклоняется), каждый шаг в своей транзакции с откатом при ошибке, таблица
  `schema_version`, отказ на базе более нового выпуска, `--check`,
  `--restore`, `--report` с числом строк каждой таблицы до/после и обрывами
  связей;
- `harness/migrations.py` (м01–м09) — часть приёмки «миграции базы», всё на
  временных базах; м09 следит, что шаги покрывают всё, что приложение
  добавляет при старте;
- инструкция оператора в `docs/ПЕРЕНОС_НА_ДРУГОЙ_ПК.md`.

Что переиспользовано: механизм `_ADDED_COLUMNS` остаётся как совместимость
(MIG-003) — приложение стартует и на немигрированной базе.

Команды / среда / exit codes:

```text
.venv/Scripts/python.exe harness/migrations.py                          → 0, 9 из 9
.venv/Scripts/python.exe scripts/check.py --quick --strict              → 0
```

Прогон на копии живой базы не выполнялся: правило `.claude/settings.json`
запрещает агенту любые команды с путём живой базы, включая снятие копии, и
обходить его не стал. Проверка на живых данных — за оператором по инструкции
в `docs/ПЕРЕНОС_НА_ДРУГОЙ_ПК.md` (`--dry-run`, затем `--apply --backup`).

Положительные случаи: м01 (dry-run), м03 (старая база мигрирована, копия
равна исходной по данным, строки целы), м04 (повтор ничего не делает), м05
(сегодняшняя база: только версия; --check зелёный), м08 (восстановление).
Отрицательные случаи: м02 (без копии — отказ, база не тронута), м06 (база
более нового выпуска не трогается), м07 (упавший шаг откатывается целиком:
версия и графы прежние).
Evidence refs / hashes: отчёты `--report` стенда во временных папках; для
копии живой базы — `dry.json`, `apply.json` в рабочей папке сессии, в
репозиторий не входят (содержат число строк живых таблиц).
Миграция / restart / rollback: живая база не мигрировалась — это отдельная
операторская операция по инструкции; откат кода — удалить `migrations/`,
`scripts/migrate.py`, стенд и часть приёмки.
Непройденное / not_run / known debt: два пути изменения схемы сосуществуют
(старт приложения и миграция); стабильные ID и mapping (MIG-001) не начаты;
T-MIG-* из ТЗ покрыты через м01–м09, не как отдельные -P/-N.
Открытое предметное решение и ответственный: нет.
Следующий конкретный шаг: L1.3 — RunManifest и неизменяемые артефакты
входа/выхода прогона по SHA-256 (VER-002), запись версии кода и зависимостей
решателя в прогон (VER-001). Закоммичено `07a4f0d`.

## Checkpoint L1.3 — снимок прогона: артефакты по SHA-256 и версия кода решателя

Дата / исполнитель: 11.09.2026, Claude (Fable).
Пакет / часть / requirement IDs: L1, часть 3; `FOT-HARNESS-TZ-001/VER-002`
(частично: вход, справочник, результат, аудит), `VER-001` (версия кода
решателя и зависимостей).
Исходный commit / состояние дерева: `07a4f0d`; чужой незакоммиченный
`scripts/test_change_guard.py` не трогался.

Что было. Прогон хранил пути к входу и результату в рабочей папке; путь —
не снимок: следующий прогон того же плана переписывает файл с тем же именем,
а версия кода решателя нигде не записывалась.

Что сделано:
- `src/fot_planner/harness_local/artifacts.py` — хранилище неизменяемых
  артефактов `app/data/artifacts/<ab>/<sha256>`: один раз записанный файл не
  переписывается, сверка — пересчёт хеша;
- `src/fot_planner/harness_local/manifest.py` — канонический JSON
  `fot-json-v1`, версия кода (SHA-256 всех .py пакета плюс версии Python и
  библиотек), черновик снимка до запуска решателя, итог после всех
  преобразований результата, сверка воспроизводимости;
- `app/main.py`: `_manifest_draft` сразу после сборки входа, `_manifest_final`
  после дозаписи листа источников в результат; ссылка `Run.manifest_sha256`
  (миграция 003); маршрут `GET /api/case/{id}/run/{run_id}/manifest` со
  сверкой;
- `harness/manifest.py` (с01–с07) — часть приёмки «снимок прогона»; стенд
  миграций теперь берёт ожидания из реестра шагов, а не из чисел в коде.

Команды / среда / exit codes:

```text
.venv/Scripts/python.exe harness/manifest.py                 → 0, 7 из 7
.venv/Scripts/python.exe harness/migrations.py               → 0, 9 из 9 (со step 003)
.venv/Scripts/python.exe scripts/check.py --quick --strict   → 0
```

Положительные случаи: с01 (артефакт по хешу, повтор не переписывает), с02
(канонический JSON), с03 (версия кода устойчива), с04 (черновик → итог,
чтение обратно), с06 (сервис записывает снимок, маршрут отдаёт со сверкой).
Отрицательные случаи: с01 (подменённые байты артефакта не проходят сверку;
не-хеш отклоняется), с02 (NaN отклонён), с03 (правка файла меняет версию),
с05 (подмена результата и пропажа входа в хранилище обнаружены; перезапись
живого файла снимку безразлична).
Evidence refs / hashes: отчёт строгого quick; сквозной прогон на временной
базе — см. ниже.
Сквозной прогон на временной базе (сервер на 8771, пустая временная папка
данных): четыре документа демо разобраны за 466 с (два перечитаны моделью),
прогон № 1 завершился «нет решения» за 3,2 с — в пустом временном реестре нет
правил замещения (в живом их 30 из должности.xlsx), и при жёстком допуске
трудоёмкости строки «Инженер» закрыть некому; это отличие окружения, не
регрессия (офлайн-счёт того же набора с правилами — OPTIMAL). Для снимка это
полезный случай: черновик записан до запуска решателя —
`GET /api/case/2/run/1/manifest` вернул снимок `095b2203…` без замечаний
сверки: вход, справочник (33 должности), 4 документа-источника, версия кода
(39 файлов), лимит 240 с; результата нет. В хранилище три артефакта: вход,
справочник, снимок. Из этого прогона вышло правило: снимок закрывается при
любом исходе, а не только при OPTIMAL (`_manifest_close` в `finally`,
случай с07); на временном сервере это ещё старый код — там остался черновик.
Отдельная находка: раньше «нет решения» при пустых правилах замещения
приходило бы как OPTIMAL с незакрытыми строками.
Миграция / restart / rollback: графа `manifest_sha256` — миграция 003 и
совместимость при старте; старые прогоны без снимка отдают 404 «посчитан до
появления снимков»; откат — убрать вызовы из `_solve`, маршрут и пакет.
Непройденное / not_run / known debt: контекст запросов к модели и версия
модели провайдера в снимок не входят; T-VER-001/002 из ТЗ
покрыты через с01–с06, не как отдельные -P/-N.
Открытое предметное решение и ответственный: нет.
Следующий конкретный шаг: L1.4 — LocalDecision: принятие строк, правки
справочника и настройки плана как решения с реальным оператором, областью и
версией предмета (VER-004, NFR-002 в части оператора). Закоммичено `3a55a63`.

## Checkpoint L1.4 — локальные решения: кто, что, над какой версией

Дата / исполнитель: 11.09.2026, Claude (Fable).
Пакет / часть / requirement IDs: L1, часть 4; `FOT-HARNESS-TZ-001/VER-004`
(частично), `NFR-002` (локальный оператор), `UI-002` (предусловие версии).
Исходный commit / состояние дерева: `3a55a63`; чужой незакоммиченный
`scripts/test_change_guard.py` не трогался.

Что было. Принятая строка, записанная норма, исключённый документ, условие
из чата оставляли только следствия — строку в реестре, число в справочнике,
флажок. Кто решил и над какой версией документа — нигде; подпись
«экономист» приходила от клиента или модели.

Что сделано:
- `src/fot_planner/harness_local/decisions.py`: оператор из `FOT_OPERATOR`
  или учётной записи ОС (не из тела запроса), хеш предмета, проверка
  устаревания версии; таблица `decisions` (миграция 004), модель `Decision`
  и `record_decision` в `app/db.py`;
- решения записываются в четырёх местах: принятие/отклонение строк
  (вид «данные», предмет — документ с хешем, действие — номера строк),
  запись величин в справочник («норма»), флажок документа из карточки и из
  чата («состав плана»), условие плана из чата («настройка плана», основание
  — реплика);
- предусловие версии: если форма прислала `document_sha256`, а документ с
  тех пор изменился, решение отклоняется с 409 и записывается как
  отклонённое — реестр не тронут (VER-004, UI-002); форма карточки
  документа шлёт хеш;
- `GET /api/decisions?case_id=|document_id=` — список, новые сверху;
- `harness/decisions.py` (р01–р07) — часть приёмки «решения».

Команды / среда / exit codes:

```text
.venv/Scripts/python.exe harness/decisions.py               → 0, 7 из 7
.venv/Scripts/python.exe scripts/check.py --quick --strict  → 0
```

Положительные случаи: р01 (оператор из переменной или ОС), р02 (решение с
хешем предмета и действием), р04 (норма), р05 (состав плана), р06 (условие из
чата с репликой как основанием), р07 (список маршрутом).
Отрицательные случаи: р01 (подпись «Шеф» из тела запроса в решение не
попадает), р03 (устаревшая форма → 409, реестр и предложения не тронуты,
отказ записан).
Миграция / restart / rollback: таблица `decisions` — миграция 004 и
`create_all` при старте; откат — убрать вызовы `record_decision`, модель,
модуль и стенд; данные решений при откате теряются, предметные данные — нет.
Непройденное / not_run / known debt: предусловие версии есть только у
карточки документа; `Verdict`/`Correction` не ссылаются на решение;
закреплённые версии для начатого прогона и отзыв версии (VER-004 целиком)
не реализованы; интерфейс решений не показывает (UI-001).
Открытое предметное решение и ответственный: нет.
Следующий конкретный шаг: проверка критериев L1 по ТЗ (раздел 9) и закрытие
пакета: повтор чистого вычисления с сохранением связи попыток (RUN-002),
`review_required` при смене зависимости (VER-003); затем L2. Закоммичено
`c708ad0`.

## Checkpoint L1.5 — актуальность, повтор, ключи операций; закрытие L1

Дата / исполнитель: 12.09.2026, Claude (Fable).
Пакет / часть / requirement IDs: L1, часть 5; `FOT-HARNESS-TZ-001/VER-003`
(частично), `RUN-001` (реплика и решение по строкам), `RUN-002` (повтор с
pins, защита от старого исполнителя), `UI-001` (актуальность расчёта).
Исходный commit / состояние дерева: `c708ad0`; чужие незакоммиченные
`scripts/change_guard.py`, `scripts/repo_state.py`, `scripts/test_change_guard.py`
и правка `scripts/stop_gate.py` не трогались.

Что было. Замена документа, правка справочника, принятые строки и условие
из чата меняли вход расчёта, а прежний план оставался «посчитан» без следа
о том, что данные ушли вперёд. Прерванный расчёт можно было только
запустить заново — по текущему реестру, то есть уже другим расчётом.
Старый исполнитель, чей прогон другой процесс закрыл как брошенный,
дописывал результат поверх. Реплика и решение по строкам при повторе
запроса выполнялись дважды.

Что сделано:
- `app/actuality.py`: отметка `review_required` с журналом причин у удачных
  прогонов, зависящих от изменения; вызывается при замене и удалении
  документа, правке справочника (оба маршрута), принятии строк, флажке
  документа, условии плана из чата; независимые прогоны не трогаются,
  неудачные — тоже; в ленту затронутого плана уходит реплика;
- `Run.review`, `Run.review_log`, `Run.retry_of`; таблица `operations`
  (миграция 005); `claim_operation`/`finish_operation` в `app/db.py`;
- `POST /api/case/{id}/run/{run_id}/retry`: повтор прерванного, не
  успевшего или упавшего расчёта по закреплённому входу из хранилища
  артефактов (`_pinned_input`), реестр не читается; попытки связаны
  `retry_of` и разделом `attempt` снимка (схема снимка 2); без снимка или
  с повреждённым артефактом — 409;
- `_solve`: после решателя прогон перечитывается; если он уже закрыт другим
  процессом — результат отброшен, в ленте сказано (RUN-002);
- `_operation`: ключ операции у реплики (`post_message`) и решения по
  строкам (`decide_proposals`); повтор — сохранённый ответ, другие данные —
  409, упавшая операция под тем же ключом не повторяется; форма шлёт ключи;
- `_save_plan_settings` больше не записывает решение и отметку, если проход
  ничего не изменил (реплика проходит и через закрепления, и через
  настройки);
- интерфейс: в таблице версий отчёта и шапке плана — «требует пересмотра»
  с причинами, «повтор № N», ссылка «повторить»;
- стенды: `harness/lifecycle.py` л08–л10, `harness/decisions.py` р08–р10.

Команды / среда / exit codes:

```text
.venv/Scripts/python.exe harness/lifecycle.py               → 0, 10 из 10
.venv/Scripts/python.exe harness/decisions.py               → 0, 10 из 10
.venv/Scripts/python.exe harness/migrations.py              → 0, 9 из 9
.venv/Scripts/python.exe harness/manifest.py                → 0, 7 из 7
.venv/Scripts/python.exe harness/chat.py                    → 0, 18 из 18
.venv/Scripts/python.exe scripts/check.py --quick --strict  → 0
```

Положительные случаи: л08 (повтор считает закреплённые байты, связь
попыток в базе и снимке, тот же ключ — тот же прогон), л10 (повтор реплики
и решения — тот же ответ, одна реплика, одна строка, одно решение),
р08–р10 (зависимые планы отмечены с причиной, результат цел, отметка в
данных плана и списке планов).
Отрицательные случаи: л08 (удачный прогон и прогон без снимка не
повторяются, прогонов не прибавилось), л09 (старый исполнитель результат
не записал, план не стал «посчитано»), л10 (другие данные под тем же
ключом — 409, конфликтная реплика в ленту не попала), р08 (независимый
план и неудачный прогон не отмечены), р09 (флажок и условие из чата не
задели чужой план; прогон без справочника в основании не отмечен нормой).
Миграция / restart / rollback: миграция 005 (три графы `runs`, таблица
`operations`) и `create_all` при старте; на живой базе оператор выполняет
`scripts/migrate.py --db app/data/fot.sqlite3 --dry-run`, затем
`--apply --backup DIR`; откат — восстановление из копии, данные отметок и
операций при откате теряются, результаты — нет.
Живая проверка: временный сервер (порт 8771, отдельная папка данных с
подготовленной базой: удачный расчёт с отметкой пересмотра и прерванный со
снимком). В браузере видны «план требует пересмотра» в шапке и в таблице
версий с причиной в подсказке, ссылка «повторить» у прерванного расчёта.
Через API: повтор № 2 создал попытку № 3 с `retry_of=2`, тот же ключ вернул
ту же попытку, повтор удачного № 1 отклонён 409; решатель на закреплённом
входе стенда упал — попытка честно «прерван», в ленте обе реплики; реплика с
одним ключом дважды — одна запись в ленте. Живая база и `app/data/` не
трогались; временная конфигурация запуска восстановлена из git.
Непройденное / not_run / known debt: нажатие ссылки «повторить» в
браузере не выполнялось (маршрут вызван напрямую); автоматического
возобновления после перезапуска нет; дата действия нормы не моделируется;
согласованная запись состояния, решения и задания — последовательными
коммитами; миграция живой базы — за оператором (`--dry-run`, затем
`--apply --backup`).
Открытое предметное решение и ответственный: нет.

### Проверка критериев L1 (ТЗ, раздел 9 и таблица тестов раздела 7)

| Тест ТЗ | Положительный | Отрицательный | Итог |
| --- | --- | --- | --- |
| T-VER-001 | версия кода и зависимостей в снимке (с03, с04) | правка любого файла пакета меняет версию (с03) | partial: модель и hints в digest не входят — решатель детерминирован, модель в расчёте не участвует |
| T-VER-002 | вход, справочник, настройки, результат восстановимы из снимка (с04–с06) | подмена байта обнаружена; путь к живому файлу снимком не считается (с05) | partial: контекст модели (hints) в снимок не входит |
| T-VER-003 | замена документа сохраняет прежнюю версию (р08) | независимый план не тронут, зависимый отмечен (р08–р10) | partial: дата действия нормы не моделируется |
| T-VER-004 | попытка считает закреплённый состав (л08) | устаревшее решение отклонено (р03); ключ применяет решение один раз (л10) | partial: версий процедур нет, отзыв версии не оформлен |
| T-RUN-001 | повтор — тот же ответ (л04, л10); занятый план — один прогон (л06) | другие данные под ключом — 409 (л05, л10); устаревшая версия — 409 (р03) | partial: версия проверяется только у документа; запись не в одной транзакции |
| T-RUN-002 | повтор с исходными pins, попытки связаны (л08) | старый исполнитель не пишет (л09); импорт не закрывает чужой прогон (л01) | partial: повтор запускает человек, не worker после перезапуска |
| T-MIG-001 | все графы и таблицы переносятся (м03, м09) | — | partial: стабильные ID и legacy_unknown не оформлены |
| T-MIG-002 | dry-run, перенос, повтор, восстановление (м01–м05, м08) | сбой на шаге откатывается (м07); новая версия отклонена (м06) | implemented |
| T-MIG-003 | SQLite без школьной БД, копия при миграции (м03) | — | partial: отдельной команды копии нет; проверки на импорт ПАИШ нет |
| T-NFR-002 | оператор из FOT_OPERATOR или ОС (р01) | подпись из тела запроса не личность (р01) | partial: области доступа не проверяются; отсутствие секретов в снимке не проверяется стендом |
| T-DOC-002 | checkpoint позволяет продолжить с пакета (этот файл) | статусы меняются только по прогону (карта, к04) | implemented для L0–L1 |

Итог: L1 закрыт как пакет с перечисленным долгом. Долг переносится:
hints модели и контекст в снимке — в L2 (MEM, каталог); дата действия
нормы — в L3 (DOM); стабильные ID миграции, отдельная копия базы и
проверка на импорт ПАИШ — в L5 (перенос и установка); версии процедур и
отзыв версии — в L4 (включение/откат). Ни один пункт долга не скрыт
статусом passed.

Следующий конкретный шаг: L2 — типизированный каталог способностей
(роли, процедуры, инструменты) с применимостью и переносом существующих
обработчиков; область действия исправлений и приговоров (MEM-001,
CAP-001..).
